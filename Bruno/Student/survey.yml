info:
  name: survey
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3000/api/survey
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
